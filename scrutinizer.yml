# .scrutinizer.yml

checks:
    php:
        code_rating: true
        duplication: true
        
build:
    tests:
        override:
            -
                command: 'phpunit --testsuite crud --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'php-clover'
